{
  "version": 1,
  "description": "git_core CI record (req-boot-bootstrap-ci-record, tap#365) — the stack this plugin's own tests run in, and the one claim per-plugin CI is allowed to make: that git_core is correct against the dependencies it DECLARES and nothing else. Install set is git_core alone (it declares no dependencies); the consumer flips the self entry to editable so the CHECKOUT under test runs, not the pinned revision. Population is empty: this plugin ships no GRIFT to seed. DELIBERATELY EXCLUDED: every other plugin, all credentials, all required_secrets, and any population step that reaches the network — so a fork can run this lane and a nightly cannot flake on a third party (req-boot-bootstrap-ci-record-5). Reproduce CI exactly: spawn --from 'git+https://github.com/unified-systems-com/git-core-tap@v0.1.0#ci' --dev-plugins git_core",
  "install": {
    "plugins": [
      {
        "slug": "git_core",
        "enabled": true,
        "source": {
          "type": "git",
          "url": "https://github.com/unified-systems-com/git-core-tap",
          "rev": "v0.1.0"
        },
        "note": "Self, pinned like any app-of-apps record — the CONSUMER (plugin-ci boot-and-test, or `spawn --dev-plugins git_core`) substitutes the checkout under test via tap.dev_workspace (req-boot-bootstrap-ci-record-4). Booting this record unflipped runs the pinned revision."
      }
    ]
  },
  "population": {
    "on_failure": "abort",
    "steps": []
  }
}
